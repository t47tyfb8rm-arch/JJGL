# 基金管家 V2.0 最终部署手册

> 服务器：124.221.184.106（公网 IP）
> OS：Ubuntu 22.04 LTS
> 部署方式：apt + systemd（不用 Docker）
> 域名：先用 IP 模式，买完域名切换

---

## 阶段总览

| 阶段 | 名称 | 状态 | 备注 |
|------|------|------|------|
| 0 | 服务器基础设施 | 📍 当前 | apt + PG/Redis/Caddy/Python/Node/Netdata |
| 1 | Caddy HTTPS（IP 模式） | 📍 当前 | 自签证书警告 |
| 2 | systemd service 模板 | 📍 当前 | 占位，等代码 |
| 3 | .env 配置 | 📍 当前 | 待填密钥 |
| 4 | 后端代码部署 | ⏳ Phase 1 | 写完代码后 |
| 5 | 前端代码部署 | ⏳ Phase 4 | 写完代码后 |
| 6 | 1.0 数据迁移 | ⏳ Phase 5 | 迁移脚本写完后 |
| 7 | 域名切换 | ⏳ 买域名后 | Let's Encrypt 自动证书 |

---

## 阶段 0：服务器基础设施

### 0.1 系统更新 + 基础工具

```bash
apt update && apt upgrade -y
apt install -y curl wget git vim htop ufw chrony ca-certificates gnupg lsb-release
```

### 0.2 时区 + NTP

```bash
timedatectl set-timezone Asia/Shanghai
systemctl enable chrony
```

### 0.3 Swap（4GB 兜底）

```bash
fallocate -l 4G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
free -h  # 确认看到 4G
```

### 0.4 防火墙

```bash
ufw default deny incoming
ufw default allow outgoing
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
ufw status
```

### 0.5 PostgreSQL 16

```bash
apt install -y postgresql postgresql-contrib
systemctl enable postgresql
systemctl start postgresql

sudo -u postgres psql <<'EOF'
CREATE USER fund WITH PASSWORD '你的强密码';
CREATE DATABASE fund OWNER fund;
GRANT ALL PRIVILEGES ON DATABASE fund TO fund;
\q
EOF

# 验证
psql -U fund -d fund -h localhost -c "SELECT version();"
```

### 0.6 Redis

```bash
apt install -y redis-server
sed -i 's/^# maxmemory <bytes>/maxmemory 96mb/' /etc/redis/redis.conf
sed -i 's/^maxmemory-policy noeviction/maxmemory-policy allkeys-lru/' /etc/redis/redis.conf
systemctl enable redis-server
systemctl restart redis-server
redis-cli ping  # PONG
```

### 0.7 Python 3.11 + venv

```bash
apt install -y python3 python3-pip python3-venv
python3 --version
```

### 0.8 Node.js 20

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
node -v && npm -v
```

### 0.9 Caddy

```bash
apt install -y debian-keyring debian-archive-keyring
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg 2>/dev/null
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/deb.debian.txt' | tee /etc/apt/sources.list.d/caddy-stable.list
apt update
apt install caddy
systemctl enable caddy
caddy version
```

### 0.10 Netdata（监控，可选但推荐）

```bash
curl https://get.netdata.cloud/kickstart.sh > /tmp/netdata-kickstart.sh
sh /tmp/netdata-kickstart.sh --stable-channel --dont-wait
# Web 面板: http://124.221.184.106:19999
```

### 0.11 项目目录

```bash
mkdir -p /opt/fund-manager/{backend,frontend,data,logs,backups}
mkdir -p /opt/fund-manager/data/uploads
mkdir -p /opt/fund-manager/data/postgres
mkdir -p /opt/fund-manager/data/redis
chown -R $USER:$USER /opt/fund-manager
```

---

## 阶段 1：Caddy HTTPS（IP 模式）

```bash
cat > /etc/caddy/Caddyfile <<'EOF'
{
    email admin@124.221.184.106
}

https://124.221.184.106 {
    tls internal
    reverse_proxy 127.0.0.1:8000 {
        header_up Host {host}
        header_up X-Real-IP {remote}
        header_up X-Forwarded-For {remote}
        header_up X-Forwarded-Proto {scheme}
    }
    encode gzip zstd
}

:80 {
    redir https://{host}{uri} permanent
}
EOF

systemctl reload caddy
systemctl status caddy --no-pager
ss -tlnp | grep -E '80|443'
```

**预期**：Caddy active (running)，端口 80/443 监听。

---

## 阶段 2：systemd service 模板（占位）

```bash
# fund-api.service
cat > /etc/systemd/system/fund-api.service <<'EOF'
[Unit]
Description=基金管家 FastAPI
After=network.target postgresql.service redis-server.service
Wants=postgresql.service redis-server.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/fund-manager/backend
EnvironmentFile=/opt/fund-manager/.env
ExecStart=/opt/fund-manager/backend/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
Restart=always
RestartSec=5
LimitNOFILE=65535
StandardOutput=append:/opt/fund-manager/logs/fund-api.log
StandardError=append:/opt/fund-manager/logs/fund-api.err.log

[Install]
WantedBy=multi-user.target
EOF

# fund-worker.service
cat > /etc/systemd/system/fund-worker.service <<'EOF'
[Unit]
Description=基金管家 Celery Worker
After=network.target postgresql.service redis-server.service
Wants=postgresql.service redis-server.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/fund-manager/backend
EnvironmentFile=/opt/fund-manager/.env
ExecStart=/opt/fund-manager/backend/venv/bin/celery -A app.tasks worker --loglevel=info --concurrency=1
Restart=always
RestartSec=5
StandardOutput=append:/opt/fund-manager/logs/fund-worker.log
StandardError=append:/opt/fund-manager/logs/fund-worker.err.log

[Install]
WantedBy=multi-user.target
EOF

# fund-beat.service
cat > /etc/systemd/system/fund-beat.service <<'EOF'
[Unit]
Description=基金管家 Celery Beat
After=network.target postgresql.service redis-server.service
Wants=postgresql.service redis-server.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/fund-manager/backend
EnvironmentFile=/opt/fund-manager/.env
ExecStart=/opt/fund-manager/backend/venv/bin/celery -A app.tasks beat --loglevel=info
Restart=always
RestartSec=5
StandardOutput=append:/opt/fund-manager/logs/fund-beat.log
StandardError=append:/opt/fund-manager/logs/fund-beat.err.log

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
# 暂不启动（等代码）
```

---

## 阶段 3：.env 配置

```bash
cat > /opt/fund-manager/.env <<'EOF'
# === 数据库 ===
DATABASE_URL=postgresql://fund:你的强密码@localhost:5432/fund
DB_USER=fund
DB_PASS=你的强密码

# === Redis ===
REDIS_URL=redis://localhost:6379/0

# === 安全（用 openssl rand -hex 32 生成） ===
SECRET_KEY=粘贴64位随机字符串
JWT_SECRET=粘贴另一个64位随机字符串
JWT_EXPIRE_HOURS=720

# === DeepSeek ===
DEEPSEEK_API_KEY=sk-你的key
DEEPSEEK_BASE_URL=https://api.deepseek.com
LLM_DAILY_QUOTA=50

# === 应用 ===
APP_NAME=基金管家
APP_VERSION=2.0.0
LOG_LEVEL=INFO
TZ=Asia/Shanghai
EOF

chmod 600 /opt/fund-manager/.env
```

**生成随机密钥**：
```bash
openssl rand -hex 32
# 复制两份分别给 SECRET_KEY 和 JWT_SECRET
```

---

## 阶段 4：后端代码部署（⏳ Phase 1 代码完成后）

```bash
# 1. 上传代码
cd /opt/fund-manager
# 从 git pull 或 scp 上传 backend/ 目录
git clone https://github.com/你的仓库.git
# 或：scp -r backend/ root@124.221.184.106:/opt/fund-manager/

# 2. 创建 venv + 装依赖
cd /opt/fund-manager/backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 3. 数据库迁移（alembic）
alembic upgrade head

# 4. 创建默认 admin
python -c "from app.core.init_db import create_default_admin; create_default_admin()"

# 5. 启动
systemctl enable --now fund-api fund-worker fund-beat

# 6. 验证
systemctl status fund-api fund-worker fund-beat
curl -k -I https://124.221.184.106/api/health
```

---

## 阶段 5：前端代码部署（⏳ Phase 4 代码完成后）

```bash
# 1. 上传 + 构建
cd /opt/fund-manager/frontend
npm install
npm run build
# 生成 dist/ 目录

# 2. 改 Caddyfile 静态文件路径
cat > /etc/caddy/Caddyfile <<'EOF'
{
    email admin@124.221.184.106
}

https://124.221.184.106 {
    tls internal

    # 前端静态资源
    root * /opt/fund-manager/frontend/dist
    encode gzip zstd

    # API 反代
    @api path /api/* /ws
    reverse_proxy @api 127.0.0.1:8000

    # SPA fallback
    try_files {path} /index.html
}

:80 {
    redir https://{host}{uri} permanent
}
EOF

systemctl reload caddy
```

---

## 阶段 6：1.0 数据迁移（⏳ Phase 5 脚本完成后）

```bash
# 1. 把 1.0 的 JSON 文件上传到服务器
mkdir -p /opt/fund-manager/migration/source
scp buy_points.json nav_history.json correction_cache.json intraday_snapshots.json \
    index_residual_cache.json multi_factor_regression.json \
    root@124.221.184.106:/opt/fund-manager/migration/source/

# 2. dry-run 演练
cd /opt/fund-manager/backend
source venv/bin/activate
python scripts/migrate_from_v1.py --dry-run

# 3. 确认无误后执行
python scripts/migrate_from_v1.py --execute

# 4. 校验
psql -U fund -d fund -h localhost -c "
SELECT 'buy_points' as t, COUNT(*) FROM buy_points
UNION ALL SELECT 'nav_history', COUNT(*) FROM nav_history
UNION ALL SELECT 'correction_samples', COUNT(*) FROM correction_samples;
"
```

---

## 阶段 7：买域名后切换（可选）

```bash
# 1. 买域名 fund.xxx.cn，加 A 记录到 124.221.184.106
# 2. 等 DNS 生效（5-10 分钟）：nslookup fund.xxx.cn
# 3. 改 Caddyfile
cat > /etc/caddy/Caddyfile <<'EOF'
{
    email you@your-email.com
}

fund.xxx.cn {
    reverse_proxy 127.0.0.1:8000 {
        header_up Host {host}
        header_up X-Real-IP {remote}
    }
    encode gzip zstd
}

:80 {
    redir https://{host}{uri} permanent
}
EOF

# 4. 重新加载（Caddy 自动申请 Let's Encrypt 证书）
systemctl reload caddy
# 等 30 秒 → 浏览器绿锁
```

---

## 验收清单

### Phase 0 验收（当前阶段）

```bash
# 在服务器上跑这个脚本，一次性验证基础设施
cat > /tmp/verify.sh <<'EOF'
#!/bin/bash
set +e
echo "=== 1. 系统信息 ==="
cat /etc/os-release | head -3
echo ""

echo "=== 2. 内存 + Swap ==="
free -h
echo ""

echo "=== 3. 磁盘 ==="
df -h / | tail -1
echo ""

echo "=== 4. 时区 ==="
date
echo ""

echo "=== 5. 防火墙 ==="
ufw status
echo ""

echo "=== 6. PostgreSQL ==="
systemctl is-active postgresql
PGPASSWORD=你的密码 psql -U fund -d fund -h localhost -t -c "SELECT version();" 2>&1
echo ""

echo "=== 7. Redis ==="
systemctl is-active redis-server
redis-cli ping
echo ""

echo "=== 8. Python ==="
python3 --version
echo ""

echo "=== 9. Node ==="
node -v
echo ""

echo "=== 10. Caddy ==="
caddy version
systemctl is-active caddy
ss -tlnp | grep -E '80|443' || echo "  80/443 未监听"
echo ""

echo "=== 11. HTTPS 自检 ==="
curl -k -s -o /dev/null -w "HTTPS 返回: %{http_code}\n" https://124.221.184.106
echo "  (502 = 正常，Caddy 工作但 fund-api 还没启)"
echo ""

echo "=== 12. Netdata ==="
ss -tlnp | grep 19999 || echo "  Netdata 未启（可选）"
echo ""

echo "=== 13. 监听端口汇总 ==="
ss -tlnp
echo ""

echo "=== 14. 进程 ==="
ps -eo pid,pcpu,pmem,rss,comm --sort=-rss | head -10
echo ""

echo "=== 完成 ==="
EOF

chmod +x /tmp/verify.sh
/tmp/verify.sh
```

### 浏览器侧验证

| 测试 | 预期 |
|------|------|
| 浏览器打开 `https://124.221.184.106` | 警告页（IP 模式）→ 点"高级"→ 继续 |
| 看到的内容 | `502 Bad Gateway`（fund-api 还没启，**正常**） |
| 买域名后打开 `https://fund.xxx.cn` | ✅ 绿锁 + 看到基金管家首页 |

---

## 关键配置汇总

| 项 | 值 |
|----|-----|
| 公网 IP | `124.221.184.106` |
| 域名 | 待定（fund.xxx.cn）|
| 服务器用户 | root |
| PostgreSQL 库/用户 | fund / fund |
| Redis | 96MB + LRU |
| 后端端口 | 8000 |
| Caddy | 80/443 自动 HTTPS |
| Netdata | 19999（监控面板）|
| 防火墙 | 22/80/443 |
| Swap | 4GB |
| 时区 | Asia/Shanghai |

---

## 待办（按时间顺序）

| # | 事项 | 谁 | 备注 |
|---|------|----|------|
| 1 | 跑完阶段 0-3 所有命令 | 你 | 当前 |
| 2 | 浏览器打开 `https://124.221.184.106` 看到 502 | 你 | 验证 Caddy 工作 |
| 3 | 买域名 + A 记录 | 你 | 28 元/年，5 分钟 |
| 4 | 改 Caddyfile 到域名模式 | 我给脚本 | DNS 生效后跑 |
| 5 | Phase 1 写后端代码 | 我（待开始）| 复用 1.0 main.py 逻辑 |
| 6 | Phase 4 写前端代码 | 我（待开始）| 1.0 HTML 拆 Vue 组件 |
| 7 | 写迁移脚本 | 我（待开始）| 1.0 JSON → PostgreSQL |
| 8 | 阶段 4-6 部署 | 你跑我给脚本 | 业务代码 ready 后 |
| 9 | 阶段 7 域名切换 | 你跑我给脚本 | 域名生效后 |
| 10 | 验收上线 | 双方 | 双重写 1 周后 |
