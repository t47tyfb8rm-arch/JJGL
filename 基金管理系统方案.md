# 基金管理系统 - 产品方案

## 1. 产品概述

### 1.1 产品定位
一款个人使用的基金投资管理工具，用于记录和管理个人持有的基金情况，并基于当日市场数据对基金净值进行估算，帮助用户实时掌握投资收益动态。

### 1.2 产品价值
- **投资可视化**：清晰展示基金持仓、收益、成本等核心数据
- **实时估算**：基于最新净值或估值数据，预估当日收益情况
- **决策辅助**：提供收益趋势、持仓分布等分析，辅助投资决策
- **数据安全**：本地数据存储，隐私安全可控

---

## 2. 目标用户

### 2.1 主要用户
- **个人基金投资者**：持有多个基金，需要统一管理持仓的用户
- **定投用户**：定期定额投资基金，需追踪定投记录和收益的用户
- **理财爱好者**：关注基金市场，希望快速了解当日收益估算的用户

### 2.2 使用场景
- **日常查看**：每日查看持仓基金的整体收益和估值
- **定投管理**：记录每期定投，追踪定投成本和收益
- **调仓记录**：记录买入、卖出操作，管理持仓变化
- **收益分析**：查看不同时间维度的收益统计和趋势

---

## 3. 功能架构

### 3.1 功能模块图

```
基金管理系统
├── 持仓管理模块
│   ├── 基金添加/删除
│   ├── 持仓份额管理
│   ├── 成本均价计算
│   └── 持仓汇总视图
├── 交易记录模块
│   ├── 买入记录
│   ├── 卖出记录
│   ├── 定投记录
│   └── 分红记录
├── 净值与估值模块
│   ├── 最新净值录入
│   ├── 当日净值估算
│   ├── 估值算法引擎
│   └── 历史净值追踪
├── 收益分析模块
│   ├── 总收益/收益率
│   ├── 持仓收益/收益率
│   ├── 日收益/累计收益
│   ├── 收益趋势图
│   └── 持仓分布图
└── 数据管理模块
    ├── 数据导入/导出
    ├── 数据备份恢复
    └── 基金信息库维护
```

### 3.2 核心功能详解

#### 3.2.1 持仓管理模块

**职责**：管理用户持有的所有基金信息及当前持仓状态。

**核心功能**：
- **基金添加**：通过基金代码搜索添加基金，自动获取基金名称、类型等信息
- **持仓编辑**：修改持有份额、成本价、买入日期等持仓信息
- **持仓汇总**：表格+卡片形式展示所有基金的持仓概览
- **分组管理**：按基金类型（股票型/混合型/债券型/货币型/指数型）分类管理

**数据结构**：
```json
{
  "funds": [
    {
      "id": "uuid",
      "code": "110011",
      "name": "易方达中小盘混合",
      "type": "混合型",
      "holdings": [
        {
          "shares": 5000.00,
          "cost_price": 3.2560,
          "buy_date": "2025-01-15",
          "source": "manual"
        }
      ],
      "current_nav": 3.4520,
      "nav_date": "2025-06-25",
      "estimated_nav": 3.4680,
      "estimated_change_pct": 0.46,
      "is_active": true,
      "notes": ""
    }
  ]
}
```

**持股成本计算**：
```
总成本 = Σ(每次买入份额 × 买入单价)
总份额 = Σ(每次买入份额)
加权成本价 = 总成本 / 总份额
```

#### 3.2.2 交易记录模块

**职责**：记录基金的所有交易操作，形成完整的交易流水。

**交易类型**：

| 类型 | 说明 | 影响 |
|------|------|------|
| 买入 | 申购/买入操作 | 增加份额，更新成本 |
| 卖出 | 赎回/卖出操作 | 减少份额，实现收益 |
| 定投 | 定期定额投资 | 自动买入，记录定投期数 |
| 分红 | 现金分红/红利再投资 | 现金分红不改变份额，红利再投资增加份额 |

**数据记录**：
```json
{
  "transactions": [
    {
      "id": "uuid",
      "fund_code": "110011",
      "type": "buy",
      "date": "2025-01-15",
      "amount": 10000.00,
      "shares": 3071.87,
      "nav": 3.2560,
      "fee": 15.00,
      "notes": "首次建仓"
    }
  ]
}
```

#### 3.2.3 净值与估值模块

**职责**：管理基金净值数据，提供当日净值估算功能。

**净值数据源**：
- **手动录入**：用户手动输入最新净值
- **自动估算**：基于算法估算当日净值

**估算算法**（多种可选）：

**方案 A：基于跟踪指数估算（推荐）**
```
适用：指数型基金、ETF 联接基金
原理：跟踪指数当日涨跌幅 × 基金Beta系数
估算净值 = 上日净值 × (1 + 跟踪指数涨跌幅 × Beta)
```

**方案 B：基于持仓估算**
```
适用：主动管理型基金
原理：基金持仓股票的当日涨跌幅加权估算
估算净值 = 上日净值 × (1 + Σ(持仓权重 × 个股涨跌幅))
前提：需要基金持仓数据，可通过定期报告获取
```

**方案 C：基于同类基金均值估算**
```
适用：信息不充分的基金
原理：同类基金当日净值变化的平均值
估算净值 = 上日净值 × (1 + 同类基金平均涨跌幅)
```

**方案 D：基于市场模型估算**
```
适用：各类基金
原理：利用历史数据建立回归模型
估算净值 = α + β₁×大盘指数 + β₂×行业指数 + ...
```

**数据记录**：
```json
{
  "nav_history": {
    "110011": [
      {
        "date": "2025-06-20",
        "nav": 3.4520,
        "change_pct": 0.32,
        "source": "manual"
      },
      {
        "date": "2025-06-25",
        "nav": 3.4680,
        "change_pct": 0.46,
        "source": "estimated",
        "estimate_method": "index_based",
        "estimate_detail": {
          "index_code": "000300",
          "index_change": 0.52,
          "beta": 0.88
        }
      }
    ]
  }
}
```

#### 3.2.4 收益分析模块

**职责**：提供多维度收益数据分析与可视化展示。

**核心指标**：

| 指标 | 公式 | 说明 |
|------|------|------|
| 持仓市值 | 当前份额 × 最新净值 | 基金当前价值 |
| 总成本 | Σ(买入金额) - Σ(卖出金额) | 投入的总金额 |
| 持仓收益 | 持仓市值 - 总成本 | 当前盈利/亏损 |
| 收益率 | (持仓收益 / 总成本) × 100% | 收益百分比 |
| 日收益 | 份额 × (当日净值 - 上日净值) | 当日盈亏 |
| 累计收益 | 持仓收益 + 已实现收益 | 总盈利/亏损 |
| 年化收益率 | (1+收益率)^(365/持有天数) - 1 | 年化回报率 |

**视图设计**：

**总览视图**：
```
┌─────────────────────────────────────┐
│  总市值: ¥168,520  总投资: ¥150,000  │
│  总收益: +¥18,520  收益率: +12.35%   │
│  今日估算收益: +¥532 (+0.32%)        │
└─────────────────────────────────────┘
```

**持仓列表**：
```
┌──────────────────────────────────────────────────────┐
│ 基金名称         持仓市值   收益率    日收益   操作    │
│──────────────────────────────────────────────────────│
│ 易方达中小盘混合  ¥68,520   +8.52%   +¥320   [查看]  │
│ 招商中证白酒指数  ¥52,300   +15.32%  +¥180   [查看]  │
│ 广发稳健增长混合  ¥47,700   +5.68%   +¥32    [查看]  │
└──────────────────────────────────────────────────────┘
```

#### 3.2.5 数据管理模块

**职责**：提供数据的导入导出和备份功能。

**功能**：
- **导出**：支持导出持仓数据、交易记录为 CSV/JSON 格式
- **导入**：从 CSV/JSON 导入持仓和交易数据
- **备份**：数据库文件的备份和恢复
- **基金库**：内置常见基金信息库，支持搜索查询

---

## 4. 技术架构

### 4.1 技术选型

#### 4.1.1 开发方案对比

根据使用场景和技术能力，提供三种方案：

| 方案 | 开发成本 | 手机查看 | 适用场景 | 核心技术栈 |
|------|---------|---------|---------|-----------|
| **A: 本地桌面应用** | ⭐⭐⭐ 高 | ❌ 不支持 | Windows 深度用户，需要高性能 | C# WPF + SQLite |
| **B: Obsidian 插件** | ⭐⭐ 中 | ✅ 通过 Obsidian 同步间接支持 | Obsidian 用户，日常用电脑管理 | TypeScript + Obsidian API |
| **C: Web 应用（推荐）** | ⭐⭐ 中 | ✅ 原生支持，手机浏览器/App直接访问 | **需要手机实时查看的用户** | Python FastAPI + Vue3/React |

> **推荐说明**：如果需要**手机端实时查看**，方案 C（Web 应用）是最直接的选择，PC 和手机都能用同一套服务，无需额外同步。方案 B（Obsidian 插件）也能通过 Obsidian 的远程同步间接在手机上查看，但体验不如 Web 应用流畅。

---

#### 4.1.2 方案 C：Web 应用（推荐，支持手机查看）

**适用场景**：需要在手机和电脑上随时查看基金持仓和当日估算收益

**技术栈**：
- **后端**：Python FastAPI / Node.js Express
- **前端**：Vue 3 + Vite / React + Vite（移动端自适应）
- **数据库**：SQLite（PC）/ PostgreSQL（自建服务器）
- **移动端适配**：Vant UI（Vue）/ Ant Design Mobile（React）
- **图表**：ECharts（支持移动端触摸交互）
- **部署**：内网穿透（frp/ngrok）或 轻量云服务器（最低 2C2G）

**架构图**：
```
┌────────────────────────────────────────────┐
│            Web 应用（同一服务）               │
│                                            │
│  ┌──────────────┐   ┌──────────────┐       │
│  │  PC 浏览器     │   │  手机浏览器    │       │
│  │  (桌面布局)    │   │  (移动布局)    │       │
│  └──────┬───────┘   └──────┬───────┘       │
│         │                   │               │
│         ▼                   ▼               │
│  ┌──────────────────────────────────────┐   │
│  │      前端 (Vue3 / React)              │   │
│  │  - 自适应布局 (响应式设计)             │   │
│  │  - 移动端触摸优化                      │   │
│  │  - PWA 离线缓存                       │   │
│  └────────────────┬─────────────────────┘   │
│                   │ REST API / WebSocket     │
│  ┌────────────────▼─────────────────────┐   │
│  │      后端 (FastAPI / Express)          │   │
│  │  - 持仓管理 API                        │   │
│  │  - 净值估算引擎                        │   │
│  │  - 指数数据抓取/录入                   │   │
│  │  - 收益计算                           │   │
│  └────────────────┬─────────────────────┘   │
│                   │                         │
│  ┌────────────────▼─────────────────────┐   │
│  │      数据库 (SQLite / PostgreSQL)      │   │
│  └──────────────────────────────────────┘   │
└────────────────────────────────────────────┘
```

**部署方案**：

| 部署方式 | 费用 | 难度 | 手机访问方式 |
|---------|------|------|------------|
| **本地部署 + 内网穿透** | 免费（frp） | ⭐⭐ 中 | 手机连同一 WiFi 或通过 frp 域名访问 |
| **云服务器部署** | ¥30-50/月（轻量） | ⭐⭐⭐ 中高 | 直接通过域名/公网 IP 访问 |
| **家用 NAS 部署** | 已有 NAS 则免费 | ⭐⭐ 中 | NAS 自带域名访问 |

**快速启动实现（Python FastAPI）**：
```python
# main.py - 最小可用版本
from fastapi import FastAPI, HTTPException
from fastapi.middleware.cors import CORSMiddleware
from pydantic import BaseModel
import sqlite3
from datetime import date

app = FastAPI(title="基金管理系统")

# 允许手机访问
app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"])

# 数据模型
class Fund(BaseModel):
    code: str
    name: str
    type: str
    shares: float      # 持有份额
    cost_price: float  # 成本价

class NavInput(BaseModel):
    code: str
    nav: float
    nav_date: str

# API 接口
@app.get("/api/portfolio")
def get_portfolio():
    """获取持仓总览（手机端首页）"""
    db = sqlite3.connect("funds.db")
    cur = db.execute("""
        SELECT f.code, f.name, f.type, 
               SUM(h.shares) as total_shares,
               AVG(h.cost_price) as avg_cost,
               n.nav as current_nav, n.nav_date
        FROM fund_info f
        LEFT JOIN holdings h ON f.code = h.fund_code
        LEFT JOIN nav_records n ON f.code = n.fund_code
            AND n.nav_date = (SELECT MAX(nav_date) FROM nav_records WHERE fund_code = f.code)
        WHERE f.is_active = 1
        GROUP BY f.code
    """)
    funds = []
    for row in cur.fetchall():
        market_value = row[3] * (row[5] or 0)
        cost = row[3] * row[4]
        profit = market_value - cost
        rate = (profit / cost * 100) if cost > 0 else 0
        funds.append({
            "code": row[0], "name": row[1], "type": row[2],
            "shares": round(row[3], 2), "avg_cost": round(row[4], 4),
            "nav": row[5], "nav_date": row[6],
            "market_value": round(market_value, 2),
            "profit": round(profit, 2), "profit_rate": round(rate, 2)
        })
    return {
        "date": date.today().isoformat(),
        "total_value": round(sum(f["market_value"] for f in funds), 2),
        "total_cost": round(sum(f["shares"] * f["avg_cost"] for f in funds), 2),
        "funds": funds
    }

@app.post("/api/nav")
def update_nav(nav: NavInput):
    """录入最新净值（手机端快速录入）"""
    db = sqlite3.connect("funds.db")
    try:
        db.execute("INSERT OR REPLACE INTO nav_records (fund_code, date, nav) VALUES (?, ?, ?)",
                   [nav.code, nav.nav_date, nav.nav])
        db.commit()
        return {"status": "ok", "message": f"{nav.code} 净值已更新"}
    finally:
        db.close()

@app.get("/api/estimate/today")
def estimate_today():
    """估算今日收益（手机端首页核心功能）"""
    # 基于最近净值 + 算法估算
    # ... 估算逻辑
    return {"estimates": [...]}

# 启动命令
# uvicorn main:app --host 0.0.0.0 --port 8000
```

**手机端界面预览**（移动自适应）：
```
┌─────────────────────┐
│  基金管家    [设置]  │
├─────────────────────┤
│ ┌───────┐ ┌───────┐ │
│ │总市值  │ │今日估  │ │
│ │¥168.5k│ │+¥532  │ │
│ └───────┘ └───────┘ │
├─────────────────────┤
│ 易方达中小盘         │
│ 市值 ¥68.5k  +8.5%  │
│ 今日 +¥320  ✓ 已估  │
├─────────────────────┤
│ 招商中证白酒         │
│ 市值 ¥52.3k  +15.3% │
│ 今日 +¥180  ○ 待估  │
├─────────────────────┤
│ [+ 添加基金]         │
│                     │
│ 📈 收益趋势  [展开]  │
└─────────────────────┘
```

---

#### 4.1.3 方案 A：本地桌面应用

**技术栈**：
- **语言/框架**：C# (.NET 8 / .NET Framework 4.8)
- **UI 框架**：WPF (Windows Presentation Foundation)
- **数据库**：SQLite (Microsoft.Data.Sqlite)
- **图表库**：LiveCharts2 / OxyPlot
- **日志**：Serilog

**项目结构**：
```
FundManager/
├── FundManager.sln
├── FundManager/
│   ├── App.xaml
│   ├── App.xaml.cs
│   ├── MainWindow.xaml
│   ├── MainWindow.xaml.cs
│   ├── Models/
│   │   ├── Fund.cs
│   │   ├── Holding.cs
│   │   ├── Transaction.cs
│   │   ├── NavRecord.cs
│   │   └── FundInfo.cs
│   ├── ViewModels/
│   │   ├── MainViewModel.cs
│   │   ├── PortfolioViewModel.cs
│   │   ├── TransactionViewModel.cs
│   │   ├── AnalysisViewModel.cs
│   │   └── SettingsViewModel.cs
│   ├── Views/
│   │   ├── PortfolioView.xaml
│   │   ├── TransactionView.xaml
│   │   ├── AnalysisView.xaml
│   │   ├── FundDetailView.xaml
│   │   └── SettingsView.xaml
│   ├── Services/
│   │   ├── IFundDataService.cs
│   │   ├── FundDataService.cs
│   │   ├── IEstimationService.cs
│   │   ├── EstimationService.cs
│   │   ├── IDataStore.cs
│   │   └── DataStore.cs
│   ├── Utils/
│   │   ├── CalculationHelper.cs
│   │   └── MarketDataHelper.cs
│   └── Resources/
│       ├── Styles/
│       └── Icons/
└── Tests/
    ├── CalculationTests.cs
    └── EstimationTests.cs
```

#### 4.1.4 方案 B：Obsidian 插件（次推荐，支持手机间接查看）

**技术栈**：
- **语言**：TypeScript
- **框架**：Obsidian Plugin API
- **数据存储**：Obsidian 本地文件系统 / IndexedDB
- **图表**：Chart.js 集成
- **数据格式**：Markdown + YAML frontmatter / JSON
- **手机方案**：配合 Obsidian Sync 或 Remotely Save 插件实现多端同步

**手机端运作方式**：
```
电脑端 (Obsidian Plugin)
    │  录入持仓、输入净值、查看估算收益
    │
    ▼
Obsidian 本地数据 (Markdown + JSON)
    │
    ▼  Obsidian Sync / Remotely Save / iCloud
    │
手机端 (Obsidian App)
    │
    ▼
查看基金持仓数据 (只读浏览)
   - 通过 Dataview 查询展示持仓表格
   - 通过自定义视图查看收益概览
```

**优劣势**：

| 维度 | 评价 |
|------|------|
| 开发成本 | ⭐⭐ 中等，Obsidian API 较完善 |
| 手机体验 | ⭐⭐ 可看但不够流畅，需 Obsidian App + 同步插件 |
| 数据安全 | ⭐⭐⭐⭐⭐ 数据全在本地，同步可自选 |
| 实时性 | ⭐⭐ 依赖 Obsidian 同步频率（通常分钟级） |
| 可扩展性 | ⭐⭐⭐ 可结合 Obsidian Dataview 做查询分析 |

**项目结构**：
```
obsidian-fund-manager/
├── manifest.json
├── main.ts
├── styles.css
├── src/
│   ├── models/
│   │   ├── fund.ts
│   │   ├── holding.ts
│   │   └── transaction.ts
│   ├── services/
│   │   ├── data-store.ts
│   │   ├── estimation-engine.ts
│   │   └── market-data.ts
│   ├── views/
│   │   ├── portfolio-view.ts
│   │   ├── fund-detail-view.ts
│   │   ├── transaction-view.ts
│   │   └── analysis-view.ts
│   └── utils/
│       ├── calculator.ts
│       └── formatter.ts
├── styles/
│   └── fund-manager.css
└── test/
```

**数据存储方式**（Markdown + YAML）：
```markdown
---
code: 110011
name: 易方达中小盘混合
type: 混合型
tags: [基金/主动管理]
current_nav: 3.4520
nav_date: 2025-06-25
---

## 持仓记录

| 日期 | 操作 | 金额 | 份额 | 净值 | 费用 | 备注 |
|------|------|------|------|------|------|------|
| 2025-01-15 | 买入 | 10,000.00 | 3,071.87 | 3.2560 | 15.00 | 首次建仓 |
| 2025-03-10 | 定投 | 1,000.00 | 302.11 | 3.3100 | 1.50 | 第1期定投 |
```

### 4.2 数据库设计（方案 C / 方案 A - SQLite）

```sql
-- 基金基础信息表
CREATE TABLE fund_info (
    code TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    management_company TEXT,
    established_date TEXT,
    tracking_index TEXT,
    is_active INTEGER DEFAULT 1
);

-- 持仓记录表
CREATE TABLE holdings (
    id TEXT PRIMARY KEY,
    fund_code TEXT NOT NULL,
    buy_date TEXT NOT NULL,
    shares REAL NOT NULL,
    cost_price REAL NOT NULL,
    source TEXT DEFAULT 'manual',  -- manual, sip, dividend
    notes TEXT,
    created_at TEXT DEFAULT (datetime('now')),
    FOREIGN KEY (fund_code) REFERENCES fund_info(code)
);

-- 交易记录表
CREATE TABLE transactions (
    id TEXT PRIMARY KEY,
    fund_code TEXT NOT NULL,
    type TEXT NOT NULL,  -- buy, sell, sip, dividend
    date TEXT NOT NULL,
    amount REAL NOT NULL,
    shares REAL NOT NULL,
    nav REAL NOT NULL,
    fee REAL DEFAULT 0,
    notes TEXT,
    created_at TEXT DEFAULT (datetime('now')),
    FOREIGN KEY (fund_code) REFERENCES fund_info(code)
);

-- 净值记录表
CREATE TABLE nav_records (
    id TEXT PRIMARY KEY,
    fund_code TEXT NOT NULL,
    date TEXT NOT NULL,
    nav REAL NOT NULL,
    change_pct REAL,
    source TEXT DEFAULT 'manual',  -- manual, estimated
    estimate_method TEXT,
    estimate_detail TEXT,
    created_at TEXT DEFAULT (datetime('now')),
    FOREIGN KEY (fund_code) REFERENCES fund_info(code),
    UNIQUE(fund_code, date)
);

-- 指数数据表（用于估值估算）
CREATE TABLE index_data (
    index_code TEXT NOT NULL,
    date TEXT NOT NULL,
    close REAL NOT NULL,
    change_pct REAL,
    PRIMARY KEY (index_code, date)
);

-- 基金关联指数表
CREATE TABLE fund_index_map (
    fund_code TEXT NOT NULL,
    index_code TEXT NOT NULL,
    beta REAL,
    r_squared REAL,
    PRIMARY KEY (fund_code, index_code),
    FOREIGN KEY (fund_code) REFERENCES fund_info(code)
);

-- 索引
CREATE INDEX idx_holdings_fund ON holdings(fund_code);
CREATE INDEX idx_transactions_fund ON transactions(fund_code);
CREATE INDEX idx_transactions_date ON transactions(date);
CREATE INDEX idx_nav_fund_date ON nav_records(fund_code, date);
```

### 4.3 核心模块设计

#### 4.3.1 估值估算引擎

**类名**：EstimationEngine

**职责**：
- 根据所选估值算法，估算基金当日净值
- 管理多种估值策略
- 记录估算准确度，优化算法

**接口设计**：
```typescript
interface IEstimationEngine {
    // 估算指定基金当日净值
    estimate(fundCode: string, date: string): Promise<EstimateResult>;
    
    // 批量估算所有持仓基金
    estimateAll(date: string): Promise<Map<string, EstimateResult>>;
    
    // 获取最近N天的估算准确度
    getAccuracy(fundCode: string, days: number): AccuracyReport;
    
    // 注册自定义估算策略
    registerStrategy(name: string, strategy: IEstimationStrategy): void;
}

interface EstimateResult {
    fundCode: string;
    fundName: string;
    date: string;
    previousNav: number;
    estimatedNav: number;
    estimatedChangePct: number;
    method: string;
    confidence: 'high' | 'medium' | 'low';
    details?: Record<string, any>;
}

interface IEstimationStrategy {
    name: string;
    estimate(fundInfo: FundInfo, context: EstimationContext): Promise<number>;
    confidence: 'high' | 'medium' | 'low';
}
```

#### 4.3.2 收益计算器

**类名**：ProfitCalculator

**职责**：
- 计算持仓收益、收益率
- 计算日收益、累计收益
- 计算年化收益率

**核心方法**：
```typescript
interface IProfitCalculator {
    // 计算持仓市值
    calculateMarketValue(shares: number, nav: number): number;
    
    // 计算总成本（加权）
    calculateTotalCost(holdings: Holding[]): number;
    
    // 计算持仓收益
    calculateProfit(marketValue: number, totalCost: number): number;
    
    // 计算收益率
    calculateProfitRate(profit: number, totalCost: number): number;
    
    // 计算日收益
    calculateDailyProfit(shares: number, currentNav: number, previousNav: number): number;
    
    // 计算年化收益率
    calculateAnnualizedRate(totalRate: number, daysHeld: number): number;
    
    // 批量计算所有基金收益
    calculateAllProfits(holdings: Map<string, number>, navs: Map<string, number>): PortfolioProfit;
}
```

---

## 5. 界面设计

### 5.1 总览页面

```
┌─────────────────────────────────────────────────────┐
│  基金管理系统                        [设置] [导出]    │
├─────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌───────────────┐  │
│  │ 总市值       │ │ 总收益       │ │ 今日估算收益    │  │
│  │ ¥168,520    │ │ +¥18,520    │ │ +¥532         │  │
│  │ 总投资¥150k │ │ +12.35%     │ │ +0.32%        │  │
│  └─────────────┘ └─────────────┘ └───────────────┘  │
├─────────────────────────────────────────────────────┤
│  持仓基金                                    [+ 添加] │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐│
│  │ 名称         市值     收益率   日收益   估值状态  ││
│  ├─────────────────────────────────────────────────┤│
│  │ 易方达中小盘  ¥68,520  +8.52%  +¥320   已估 ✓  ││
│  │ 招商中证白酒  ¥52,300  +15.32% +¥180   待估 ○  ││
│  │ 广发稳健增长  ¥47,700  +5.68%  +¥32    已估 ✓  ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  收益趋势 [近1周][近1月][近3月][近6月][近1年][自定义] │
│  ┌─────────────────────────────────────────────────┐│
│  │                                                   ││
│  │        📈 收益曲线图                               ││
│  │                                                   ││
│  └─────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

### 5.2 基金详情页面

```
┌─────────────────────────────────────────────────────┐
│  ← 返回  易方达中小盘混合 (110011)                    │
├─────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │ 持仓市值  │ │ 持有收益  │ │ 收益率   │ │ 日收益  │ │
│  │ ¥68,520 │ │ +¥5,380 │ │ +8.52%  │ │ +¥320  │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
├─────────────────────────────────────────────────────┤
│  ┌──── 净值走势 ──────────────────────────────────┐  │
│  │                                                   │
│  │        📈 净值曲线 + 估算值(虚线)                   │
│  │                                                   │
│  └─────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────┤
│  持仓明细                                       [+ 新增]│
│  持有份额: 15,000.00份                              │
│  加权成本: ¥3.2560  |  最新净值: ¥3.4520            │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐│
│  │ 日期       操作    金额       份额    净值   备注 ││
│  │ 2025-01-15 买入  ¥10,000  3,071.87  3.2560 建仓 ││
│  │ 2025-03-10 定投  ¥1,000    302.11  3.3100 第1期 ││
│  │ ...                                               ││
│  └─────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────┤
│  估值设置                                           │
│  估值算法: [基于跟踪指数]                            │
│  跟踪指数: 沪深300 (000300)                         │
│  Beta系数: 0.88                                     │
│  估值置信度: 高                                     │
└─────────────────────────────────────────────────────┘
```

### 5.3 交互流程

#### 5.3.1 添加基金流程
1. 点击"添加"按钮，弹出添加窗口
2. 输入基金代码，自动带出基金名称和基本信息
3. 输入买入金额、份额、净值等持仓信息
4. 选择或配置估值算法
5. 点击确认，基金加入持仓列表

#### 5.3.2 每日估值流程
1. 打开系统，自动检查所有持仓基金的最新净值日期
2. 对需要估算的基金，根据配置的算法进行净值估算
3. 更新持仓市值和收益数据
4. 在界面展示今日收益估算

#### 5.3.3 录入真实净值
1. 基金公司公布最新净值后，用户手动录入
2. 系统自动对比估算值与真实值的偏差
3. 更新收益数据，并记录估算准确度

---

## 6. 数据流

```
用户输入/操作                 系统自动/估算
    │                            │
    ▼                            ▼
┌──────────────┐      ┌──────────────────┐
│  用户界面      │      │  估值估算引擎      │
│  (输入/展示)   │◄────►│  (算法计算)        │
└──────┬───────┘      └────────┬─────────┘
       │                       │
       ▼                       ▼
┌─────────────────────────────────────────┐
│          业务逻辑层                       │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐  │
│  │持仓管理  │ │交易管理   │ │收益计算   │  │
│  └────┬────┘ └────┬─────┘ └────┬─────┘  │
│       │           │            │         │
└───────┼───────────┼────────────┼─────────┘
        ▼           ▼            ▼
┌─────────────────────────────────────────┐
│          数据存储层 (SQLite/JSON)         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │基金信息表 │ │持仓表     │ │净值记录表 │  │
│  └──────────┘ └──────────┘ └──────────┘  │
│  ┌──────────┐ ┌──────────┐               │
│  │交易记录表 │ │指数数据表 │               │
│  └──────────┘ └──────────┘               │
└─────────────────────────────────────────┘
```

---

## 7. 实施计划

### 7.1 开发阶段（推荐方案 C：Web 应用，支持手机端）

#### 第一阶段：后端核心（1周）
- [ ] FastAPI 项目搭建和数据库初始化
- [ ] SQLite 表结构创建
- [ ] 基金基础信息 API（增删改查）
- [ ] 持仓管理 API
- [ ] 交易记录 API

#### 第二阶段：净值与估值（3天）
- [ ] 净值录入 API
- [ ] 净值估算 API（基于跟踪指数算法）
- [ ] 指数数据管理
- [ ] 收益计算逻辑

#### 第三阶段：前端基础（1周）
- [ ] Vue3/React 项目搭建
- [ ] PC 端持仓总览页面
- [ ] 基金详情页面
- [ ] 添加/编辑表单

#### 第四阶段：手机端适配（3天）
- [ ] 移动端响应式布局
- [ ] 手机端首页（总市值 + 今日估算收益 + 持仓列表）
- [ ] 手机端快速录入净值
- [ ] 触摸交互优化

#### 第五阶段：部署与完善（3天）
- [ ] 内网穿透/服务器部署
- [ ] PWA 离线缓存
- [ ] 图表展示（收益趋势、持仓分布）
- [ ] 数据导入导出功能

### 7.2 开发阶段（方案 A：桌面应用）
- [ ] 设计数据模型和数据库表结构
- [ ] 实现 SQLite 数据访问层
- [ ] 实现基金基础信息管理
- [ ] 基础数据导入导出功能

#### 第二阶段：持仓管理（1周）
- [ ] 基金添加/编辑功能
- [ ] 持仓份额管理
- [ ] 交易记录录入
- [ ] 成本计算

#### 第三阶段：净值与估值（1.5周）
- [ ] 净值录入功能
- [ ] 多种估值算法实现
- [ ] 指数数据管理
- [ ] 估值准确度追踪

#### 第四阶段：收益分析（1周）
- [ ] 收益指标计算
- [ ] 持仓收益展示
- [ ] 收益趋势图表
- [ ] 持仓分布图表

#### 第五阶段：界面完善（0.5周）
- [ ] 总览页面
- [ ] 基金详情页面
- [ ] 设置页面
- [ ] 交互优化

### 7.3 开发阶段（方案 B：Obsidian 插件）

#### 第一阶段：插件骨架（3天）
- [ ] 搭建 Obsidian 插件项目
- [ ] 实现设置面板
- [ ] 数据存储方案设计

#### 第二阶段：基金管理（4天）
- [ ] 添加/编辑基金命令
- [ ] 持仓信息管理
- [ ] 交易记录管理

#### 第三阶段：估值功能（4天）
- [ ] 净值录入
- [ ] 估值算法实现
- [ ] 每日估算

#### 第四阶段：视图展示（3天）
- [ ] 持仓总览视图
- [ ] 基金详情视图
- [ ] 收益图表

---

## 8. 风险与应对

### 8.1 数据风险
**风险**：净值数据不准确，影响收益计算
**应对**：
- 明确标注"估算"与"真实"净值的区别
- 记录估算方法和置信度
- 支持用户手动修正净值数据

### 8.2 估值准确性风险
**风险**：估值算法与实际净值偏差较大
**应对**：
- 提供多种估值算法供用户选择
- 记录历史估算准确度
- 建议用户优先选择基于跟踪指数的算法

### 8.3 数据丢失风险
**风险**：本地数据因意外而丢失
**应对**：
- 支持数据导出（CSV/JSON）
- 支持定期自动备份
- 数据存储在标准格式，便于恢复

### 8.4 合规风险
**风险**：涉及基金数据的获取和使用
**应对**：
- 不自动爬取第三方网站数据
- 用户手动录入净值，或使用公开API
- 数据仅用于个人投资参考，不构成投资建议

---

## 9. 成功指标

### 9.1 用户指标
- 个人使用满意度 > 4.5/5.0
- 每日使用频率 ≥ 1次
- 持仓基金录入完整度 > 95%

### 9.2 技术指标
- 启动时间 < 500ms（桌面应用）
- 估值计算时间 < 1s
- 数据存储占用 < 50MB

### 9.3 功能指标
- 估值算法准确度（与实际净值偏差 < 0.5%）
- 核心功能完整度 > 95%
- 数据持久化稳定性 > 99.9%

---

## 10. 后续优化方向

### 10.1 短期优化（1-3个月）
- 添加更多估值算法（机器学习模型）
- 支持基金净值的定时自动估算
- 添加收益提醒（达到止盈/止损点）
- 支持多币种基金

### 10.2 中期优化（3-6个月）
- 支持基金对比分析
- 投资组合再平衡建议
- 定投计划制定和管理
- 支持导入第三方平台数据（蚂蚁财富、天天基金等）

### 10.3 长期优化
- 资产配置分析和建议
- 风险指标计算（最大回撤、夏普比率等）
- 投资报告自动生成
- Web 端同步查看

---

## 11. 附录

### 11.1 术语表

| 术语 | 说明 |
|------|------|
| 净值 (NAV) | 基金单位净值，每份基金的价值 |
| 持仓 | 当前持有的基金份额 |
| 收益率 | 投资收益与投入成本的百分比 |
| 定投 | 定期定额投资基金的方式 |
| Beta系数 | 衡量基金相对于基准指数的波动性 |
| 估值 | 基于模型对基金净值的预估计算 |
| 年化收益率 | 将收益率换算为一年的回报率 |

### 11.2 常见基金类型

| 类型 | 风险等级 | 特点 | 估值方法建议 |
|------|---------|------|-------------|
| 股票型基金 | 高 | 股票仓位≥80% | 基于跟踪指数 |
| 混合型基金 | 中高 | 股债混合配置 | 基于持仓/同类均值 |
| 债券型基金 | 中低 | 债券仓位≥80% | 基于同类均值 |
| 货币型基金 | 低 | 货币市场工具 | 万份收益估算 |
| 指数型基金 | 视指数 | 跟踪特定指数 | 基于跟踪指数 |
| ETF联接基金 | 视标的 | 投资ETF | 基于跟踪指数 |

---

## 12. 手机端实时查看方案详解

### 12.1 需求分析

用户核心诉求：**在手机上实时查看基金持仓和当日收益估算**，而不是需要打开电脑。

关键需求点：
1. **快速浏览**：手机打开就能看到总市值、今日估算收益、各基金收益
2. **双向操作**：能在手机上录入真实净值，也能查看过往记录
3. **实时性**：数据在 PC 录入后，手机能较快同步看到

### 12.2 三种方案的手机端对比

| 维度 | A: WPF 桌面应用 | B: Obsidian 插件 | C: Web 应用（推荐） |
|------|---------------|----------------|------------------|
| **手机能否查看** | ❌ 不能 | ✅ 能（需 Obsidian App） | ✅ 能（手机浏览器/App） |
| **查看流畅度** | — | ⭐⭐ 一般 | ⭐⭐⭐⭐⭐ 原生流畅 |
| **数据同步方式** | — | Obsidian Sync / iCloud / 第三方同步 | 同一个服务器直接访问 |
| **数据实时性** | — | 分钟级（同步周期） | **秒级（实时）** |
| **手机能否录入** | — | ❌ 难以操作 | ✅ 可录入净值 |
| **是否需要额外服务** | — | ✅ 需 Obsidian 同步服务（¥10/月） | ✅ 需要部署服务 |
| **离线能否用** | ✅ 能 | ✅ 能（同步后） | ✅ 可做 PWA 离线缓存 |
| **维护成本** | 低 | 低 | 中（需维护服务器） |

### 12.3 推荐路径：Web 应用方案详解

#### 整体架构

```
┌─────────────────────────────────┐
│         用户层                    │
│  ┌─────────┐   ┌─────────┐      │
│  │ PC 浏览器│   │ 手机浏览器 │      │
│  │ (管理录入)│   │ (快速查看) │      │
│  └─────┬───┘   └────┬──────┘      │
└────────┼─────────────┼─────────────┘
         │             │
         ▼             ▼
┌─────────────────────────────────┐
│   Nginx (反向代理 / 静态资源)     │
└─────────────┬───────────────────┘
              │
┌─────────────▼───────────────────┐
│   FastAPI 后端服务               │
│   ├── /api/portfolio   持仓总览  │
│   ├── /api/estimate    估算收益  │
│   ├── /api/fund        基金管理  │
│   ├── /api/nav         净值录入  │
│   └── /api/transaction 交易管理  │
└─────────────┬───────────────────┘
              │
┌─────────────▼───────────────────┐
│         SQLite 数据库            │
└─────────────────────────────────┘
```

#### 部署到手机的两种方式

**方式一：本地 PC 运行 + 手机访问（推荐入门）**
```
步骤：
1. PC 启动 FastAPI 服务
2. 手机和 PC 连同一 WiFi
3. 手机浏览器访问 http://PC局域网IP:8000
4. ⚡ 无需公网，纯局域网秒级响应

优点：免费、安全、无需服务器
缺点：PC 需开机，离开家/办公室的网络后无法访问
```

**方式二：云服务器部署（推荐长期使用）**
```
步骤：
1. 购买轻量云服务器（腾讯云轻量 ¥30-50/月）
2. 部署 FastAPI + 前端静态资源
3. 通过域名/公网 IP 访问
4. ⚡ 随时随地，4G/5G 都能访问
5. 可配置 HTTPS 保证安全

优点：7×24小时在线，任何网络都能访问
缺点：有月费，需要简单的服务器运维
```

#### 手机端核心页面设计

**首页 - 持仓总览**：
```
┌─────────────────────────┐
│  09:30                  │
│  基金管家    📊 今日估算 │
├─────────────────────────┤
│  ┌─────────────────────┐│
│  │ 总市值  ¥168,520    ││
│  │ 今日估  +¥532(+0.32%)││
│  │ 累计收  +¥18,520    ││
│  └─────────────────────┘│
├─────────────────────────┤
│  易方达中小盘混合        │
│  1.5万份 × 3.4520       │
│  市值 ¥68,520  +8.52%   │
│  今日 +¥320  ✓ 已估     │
│  ─────────────────────  │
│  招商中证白酒指数        │
│  1.2万份 × 4.3583       │
│  市值 ¥52,300  +15.32%  │
│  今日 +¥180  ○ 待算     │
│  ─────────────────────  │
│  广发稳健增长混合        │
│  2.8万份 × 1.7036       │
│  市值 ¥47,700  +5.68%   │
│  今日 +¥32   ✓ 已估     │
├─────────────────────────┤
│  [+ 添加基金]  [录入净值] │
└─────────────────────────┘
```

**净值录入页（手机端快速操作）**：
```
┌─────────────────────────┐
│  ← 返回    录入净值     │
├─────────────────────────┤
│  选择基金               │
│  [易方达中小盘混合 ▼]   │
│                         │
│  净值日期               │
│  [2025-06-26]           │
│                         │
│  单位净值               │
│  [3.4680]               │
│                         │
│  ┌─────────────────────┐│
│  │  上次估值: 3.4650   ││
│  │  偏差: +0.09% ✓     ││
│  └─────────────────────┘│
│                         │
│  ┌────────────────────┐ │
│  │    ✅ 确认录入      │ │
│  └────────────────────┘ │
└─────────────────────────┘
```

### 12.4 快速实现方案

如果你希望**最快**在手机上看到效果，推荐以下渐进式实现路径：

#### 第1步：纯前端静态页面（当天可用）
```
最简单的方式：
1. 创建一个 HTML 文件（前端）
2. 数据先写死 JSON 或存 LocalStorage
3. 手机浏览器直接打开即可查看

适合：先体验界面效果，不需要联网
```

#### 第2步：本地 + 局域网（1天）
```
1. 用 Python FastAPI 写 3-5 个核心 API
2. 前端用 Vue3 做移动端自适应页面
3. PC 跑服务，手机局域网访问

适合：在家/办公室使用
```

#### 第3步：云端部署（2-3天）
```
1. 购买轻量云服务器
2. 部署后端和前端
3. 配置域名和 HTTPS
4. 手机随时随地访问

适合：长期使用
```

### 12.5 关键实现技巧

#### 数据索引获取
手机端估算净值需获取实时行情数据，推荐以下途径（用户自行配置）：

| 数据源 | 获取方式 | 免费额度 | 适合 |
|-------|---------|---------|------|
| **东方财富 API** | HTTP 请求公开接口 | 免费 | 指数行情、基金净值 |
| **天天基金 API** | HTTP 请求公开接口 | 免费 | 基金基本信息 |
| **AkShare** (Python库) | pip install akshare | 免费 | 批量获取数据 |
| **Tushare Pro** | Python SDK | 200分/天免费 | 专业数据需求 |

**示例：获取指数实时行情（手机端后端代理请求）**
```python
import httpx
from fastapi import APIRouter

router = APIRouter()

@router.get("/api/market/index/{code}")
async def get_index_realtime(code: str):
    """获取指数实时行情，用于净值估算"""
    # 示例：获取沪深300（000300）实时数据
    url = f"https://push2.eastmoney.com/api/qt/stock/get"
    params = {
        "secid": f"1.{code}",
        "fields": "f43,f44,f45,f46,f47,f48,f50,f51,f57,f58,f170"
    }
    async with httpx.AsyncClient() as client:
        resp = await client.get(url, params=params)
        data = resp.json()["data"]
        return {
            "code": code,
            "name": data["f58"],
            "current": data["f43"] / 100,      # 当前价
            "open": data["f44"] / 100,          # 开盘价
            "high": data["f45"] / 100,           # 最高
            "low": data["f46"] / 100,            # 最低
            "change_pct": data["f170"] / 100,    # 涨跌幅%
            "time": data["f50"]                  # 更新时间
        }

@router.get("/api/estimate/all")
async def estimate_all_funds():
    """估算所有持仓基金当日净值和收益"""
    # 1. 获取所有持仓基金
    # 2. 对每个基金选择合适的估算算法
    # 3. 获取对应指数实时行情
    # 4. 计算估算净值
    # 5. 计算收益
    # 6. 返回结果
    pass
```

#### 离线缓存（PWA）
```
为了让手机端在网络不佳时也能看：
1. 前端启用 Service Worker
2. 缓存最后一次获取的持仓数据
3. 离线时展示缓存数据 + 标注"数据可能延迟"
4. 网络恢复时自动更新
```

---

## 13. 总结与建议

### 快速上手路线图

```
如果你想要：
    │
    ├── 最快看到效果 → 第1步：纯前端 HTML（当天完成）
    │                     手机打开即可看，数据先手动维护
    │
    ├── 日常使用 + 局域网 → 第2步：FastAPI + Vue3（1-2天）
    │                         PC 当服务器，手机局域网查看
    │
    └── 随时随地方便用 → 第3步：云部署 + 域名（3天）
                           手机 4G/5G 随时查看
```

### 方案选择建议

| 你的情况 | 推荐方案 | 理由 |
|---------|---------|------|
| 只用电脑，不需要手机看 | 方案 A：WPF 桌面应用 | 性能好，操作顺手 |
| 已深度使用 Obsidian | 方案 B：Obsidian 插件 | 与笔记体系融合 |
| 需要手机实时查看 | **方案 C：Web 应用** | 手机浏览器直接访问 |
| 想先快速试下效果 | 纯前端 HTML 页面 | 当天就能在手机上看 |

### 最终推荐

**如果需要手机实时查看，最务实的路径是：**

先用 **纯前端 HTML 页面**（当天完成）在手机上看看界面效果 → 觉得 OK 后再搭 **FastAPI 后端** 实现真正的数据管理和净值估算 → 最后部署到 **云服务器** 实现随时随地访问。

这样每一步都有实际产出，不会一次性投入太大。
